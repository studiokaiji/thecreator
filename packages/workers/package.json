{
  "name": "workers",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4",
    "wrangler": "2.1.15"
  },
  "scripts": {
    "build": "esbuild --bundle --outdir=dist ./src/index.ts",
    "start": "wrangler dev",
    "dev": "wrangler dev --local -e local",
    "publish": "wrangler publish",
    "test": "env TS_NODE_COMPILER_OPTIONS='{\"module\": \"commonjs\" }' mocha -r ts-node/register -r tsconfig-paths/register 'tests/**/*.ts' --exit"
  },
  "private": true,
  "dependencies": {
    "@honojs/firebase-auth": "^1.0.1",
    "ethers": "^5.7.2",
    "hono": "^2.5.0"
  }
}
